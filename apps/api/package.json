{
  "name": "@bim/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@bim/shared": "file:../../packages/shared",
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
